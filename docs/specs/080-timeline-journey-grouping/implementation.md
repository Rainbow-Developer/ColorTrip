# [구현 수준] 여행 타임라인 화면 — 여행별 그룹핑

| 항목 | 내용 |
|------|------|
| 상태 | 완료 |
| 최종 업데이트 | 2026-08-11 |

## 구현 규모 / 단위 분할
- **규모 판단**: 한 번에 구현 — 근거: 백엔드 변경이 없고, 건드리는 화면이 `timeline_screen.dart`·`travel_list_screen.dart`(위젯 공유화)·`core/widgets/trip_card.dart`(신규) 세 곳뿐이라 단위로 쪼갤 만큼 크지 않다.

## 구현된 항목
- [x] `TripCard`를 `core/widgets/trip_card.dart`로 공유 위젯 승격, `travel_list_screen.dart`가 재사용
- [x] 월 필터링된 entries를 지역별로 묶는 로직(`_groupByRegion`), 그룹 내 가장 이른 완료 시각 기준 오름차순 정렬
- [x] 월 선택 UI를 가로 스크롤 pill(`_MonthPillSelector`)로 교체 — 데이터가 있는 가장 이른 달 또는 최근 3개월 중 더 넓은 범위부터 이번 달까지
- [x] 지역별 보기(`_RegionGroupedView`, `_RegionTimelineSection`) — 지역별 접기/펼치기 섹션(`ExpansionTile`), 참고 디자인에 맞춰 카드 스타일을 테두리 대신 흰 배경+그림자로 조정, 날짜 라벨 옆 상태 점, "여행중"을 배지(pill)로 표시
- [x] 월 요약 카드(`_MonthSummaryCard`) — "N개 지역 · M개 퀘스트", 진행 바 + 우측 정렬 "done/total 완료", 빈 달의 "0개 지역 · 0개 퀘스트" 상태 포함
- [x] 에뮬레이터 실기 확인: 지역별 보기, 빈 달 상태, 접기/펼치기 모두 스크린샷으로 검증
- [x] `flutter test` 전체 스위트(107개) — 관련 없는 기존 실패 1건(날짜 피커 플레이키) 제외 전부 통과

## 미구현 / 남은 항목
- (없음 — 이번 요청 범위는 모두 구현됨)

## 알려진 한계 / TODO
- **여행별 보기는 제거됨.** 처음엔 우상단 토글로 여행별/지역별 보기를 전환하도록 구현했으나("의사결정" 표 참고), 이후 요청으로 토글과 여행별 보기(`_JourneyGroupedView`, `_groupByJourney`, `_JourneyGroup`, `_UnclassifiedGroupHeader`)를 제거하고 **지역별 보기를 유일한 화면**으로 확정했다. `TripCard` 공유 위젯 자체는 `travel_list_screen.dart`에서 계속 쓰여 남아있다.
- 한 퀘스트가 여러 여행의 `questKeys`에 동시에 걸리는 경우 먼저 매칭되는 여행에 귀속되는 단순 처리(plan.md 리스크 항목 참고, `_groupByRegion`은 지역 기준이라 이 이슈와 무관). 실제 이슈 보고 시 재검토.
- 참고 디자인의 퀘스트 카드 부분 진행 표시("1/2" 등), "예정"(미래) 여행 노출, 미니 지도 프리뷰, "이 날짜에 여행 추가하기" 버튼은 이번 범위에서 제외(plan.md 비목표 참고).
- `_MonthPillSelector`의 pill 라벨은 "M월"만 표시하고 연도를 표시하지 않아, 데이터가 여러 해에 걸치면 라벨이 겹칠 수 있음(내부 정렬/필터 키는 연도 포함이라 동작은 정확하나 표시상 모호함 — 실사용에서 발생하면 라벨에 연도 표기 추가 검토).

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-08-11 | 최초 작성 (계획 단계) |
| 2026-08-11 | 여행별 그룹핑 + 지역별 보기 토글 구현, 실기 검증 완료 |
| 2026-08-11 | 참고 디자인 요청으로 토글·여행별 보기 제거, 지역별 보기 단일화 + 카드 스타일을 참고 디자인에 맞게 조정(그림자, 상태 점, 배지) |
