# [구현 수준] 지도 채색 기준 전환 — 완료 여행 수

| 항목 | 내용 |
|------|------|
| 상태 | 완료 |
| 최종 업데이트 | 2026-07-31 |

## 구현 규모 / 단위 분할

- **규모 판단**: 한 번에 구현 — 근거: BE 집계 쿼리 1개 + 응답 필드 1개, FE 상태 로직 교체로 범위가 좁고 상호 의존이 단순하다.

## 구현된 항목

- [x] BE 완료 여정 집계(`count_completed_journeys_by_region`) + `MapProgressRead.completed_journey_count` 응답
- [x] FE 채도·통계 로직 전환 — `completedTripCountOf`(서버·로컬 max 병합), cap=3, `completedRegionCount`=1회 이상
- [x] FE 로컬 완주 카운터 — 완주 시점에 지역별로 누적해, 완료 지역에 퀘스트를 추가해도 채색이 되돌아가지 않는다
- [x] 테스트 — BE `tests/test_map_flow.py`(여정 0/1/2회·타 유저 격리), FE `test/map_coloring_test.dart`

## 미구현 / 남은 항목

- [ ] (후속) 로컬 완주 카운터의 앱 재시작 영속화 — 현재 전역 상태와 함께 메모리에만 남는다

## 알려진 한계 / TODO

- 로컬 완주 카운터는 메모리 상태라 앱 재시작 시 초기화된다(서버 동기화 값은 다시 받아온다).
- `MapProgress.completed_count`(완료 퀘스트 수)는 하위호환을 위해 응답에 유지한다.

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-30 | 최초 작성 (KAN-58) |
| 2026-07-31 | 구현 완료. 검증에서 발견한 채색 회귀(완료 지역에 퀘스트 추가 시 채도 0으로 되돌아감) 수정 — 로컬 파생값을 "현재 선택 집합 완주"에서 "누적 완주 횟수"로 변경 |
