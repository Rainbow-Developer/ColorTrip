# [구현 수준] 지도 채색 기준 — 퀘스트를 1개 이상 완료한 여행 수

| 항목 | 내용 |
|------|------|
| 상태 | 완료 (KAN-58 / KAN-73 채색 단위 재정의) |
| 최종 업데이트 | 2026-08-13 |

## 구현 규모 / 단위 분할

- **규모 판단**: 한 번에 구현 — 근거: BE 집계 쿼리 1개 + 응답 필드 1개, FE 상태 로직 교체로 범위가 좁고 상호 의존이 단순하다.
- **KAN-73 개정 규모**: 한 번에 구현 — BE 집계 쿼리 1개 교체 + FE 누적 시점 1곳 변경. 여행 `status` 판정 변경([010-journey](../010-journey/))과 같은 브랜치에서 진행하지만 서로 독립적이다.

## 구현된 항목

- [x] BE 여정 집계 + `MapProgressRead.completed_journey_count` 응답
- [x] FE 채도·통계 로직 전환 — `completedTripCountOf`(서버·로컬 max 병합), cap=5, `completedRegionCount`=1회 이상
- [x] FE 로컬 채색 카운터 — 지역별로 누적해, 완료한 지역에 퀘스트를 추가해도 채색이 되돌아가지 않는다
- [x] 테스트 — BE `tests/test_map_flow.py`(여정 0/1/2회·타 유저 격리), FE `test/map_coloring_test.dart`
- [x] (KAN-73) BE 집계 교체 — `count_colored_journeys_by_region`: `Journey ⨝ JourneyQuest ⨝ QuestProgress(completed)`의 `COUNT(DISTINCT Journey.id)` (`app/maps/repository.py`)
- [x] (KAN-73) FE 로컬 누적 시점 교체 — "선택 퀘스트 전부 완주" → "그 지역 여행에서 첫 퀘스트 완료" (`progress_notifier.dart`)
- [x] (KAN-73) 테스트 — BE `tests/test_map_flow.py`(퀘스트 3개 중 1개만 완료한 진행중 여행이 1회, 한 여행에서 2개 완료해도 1회), FE `test/map_coloring_test.dart`(5개 중 1개 완료 시 채도 1/5, 같은 여행 추가 완료 시 1회 유지)

## 미구현 / 남은 항목

- [ ] (후속) 로컬 채색 카운터의 앱 재시작 영속화 — 현재 전역 상태와 함께 메모리에만 남는다
- [ ] (후속) 로컬 카운터는 같은 지역 재방문(새 여정)을 구분하지 못한다 — 지역당 여행 하나만 투영하기 때문이며, 서버 동기화 값이 정확한 횟수를 준다(max 병합)

## 알려진 한계 / TODO

- 로컬 채색 카운터는 메모리 상태라 앱 재시작 시 초기화된다(서버 동기화 값은 다시 받아온다).
- `MapProgress.completed_count`(완료 퀘스트 수)는 하위호환을 위해 응답에 유지한다.
- 응답 필드명 `completed_journey_count`는 KAN-73 이후 의미가 "채색으로 집계된 여행 수"로 넓어진다(개명하지 않음 — plan 의사결정 참고).

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-30 | 최초 작성 (KAN-58) |
| 2026-07-31 | 구현 완료. 검증에서 발견한 채색 회귀(완료 지역에 퀘스트 추가 시 채도 0으로 되돌아감) 수정 — 로컬 파생값을 "현재 선택 집합 완주"에서 "누적 완주 횟수"로 변경 |
| 2026-08-13 | KAN-73 — 채색 카운트 단위를 "완료 여행 수"에서 "퀘스트를 1개 이상 완료한 여행 수"로 재정의(사용자 확정)하고 구현·검증 완료. 채색을 여행 `status`와 분리. BE 집계 쿼리 교체 + FE 누적 시점 변경, BE/FE 테스트 추가 |
