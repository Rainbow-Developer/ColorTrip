# [구현 수준] 다채로울지도 프론트엔드 앱 (Flutter)

| 항목 | 내용 |
|------|------|
| 상태 | 진행 중 (U0~U4 1차 구현 완료 — 최소 버전, Figma 기반 화면 정합 진행 중, 추적 티켓 KAN-021) |
| 최종 업데이트 | 2026-07-23 |

## 코드 유실 (중요 — 2026-07-05 재구현으로 해소)

과거 루트 `.gitignore`의 Python 템플릿 규칙(`lib/`)이 `frontend/lib`(Flutter 소스)를 오인 무시해, 아래 "구현 완료"로 기록됐던 13화면 코드가 **한 번도 커밋되지 않고 유실**되었다. gitignore 버그 자체는 `c9c00b6`(`fix(repo): 루트 .gitignore의 모노레포 오인 무시 규칙 제거`)에서 고쳐졌지만, 소스 재작성은 이뤄지지 않은 채 방치되어 있었다. **KAN-021**을 추적 티켓으로 U0~U4를 재구현해 2026-07-05 1차 구현이 완료되었고, `frontend/lib/`은 **현재 저장소에 추적되어 존재한다**(아래 변경 이력 참고). 요구사항·설계(`plan.md`)는 유실 전후 동일하게 유효하다.

## 구현 규모 / 단위 분할

- **규모 판단**: **단위로 나눠 구현**. 근거 — 13개 화면 + 인증 플로우 + 지도 렌더링은 한 번에 다루기 큰 규모이고, 화면군이 서로 독립적이라 feature 단위로 나누면 서브에이전트 병렬 구현이 가능하다([AGENT_GUIDE 작업 워크플로우](../../AGENT_GUIDE.md) · 개인지침 18).
- **구현 단위** (순서대로 — U0는 공통 기반이라 선행, U1~U4는 U0 위에서 병렬 가능. KAN-021은 U0(공통 컴포넌트)부터 우선 진행):
  - [x] **U0 스캐폴드 (선행)** — `frontend/lib` 생성, 디자인 토큰 테마, GoRouter 라우트·앱 셸(홈/퀘스트/마이 탭), core 공용 위젯(ChungbukMap·토스트·유형 배지·필터 칩), 정적 데이터·모델·Repository(지역 11·퀘스트 18·DNA 5·설문 4), 공통 Riverpod 상태(진행도·완료·타임라인·DNA). pre-commit `dart format`/`flutter analyze` 네이티브 동작 확인. **완료.**
  - [x] **U1 온보딩+설문** — 스플래시·회원가입·초기설문(4문항, 최다 선택 유형 집계)·DNA결과. **1차 구현 완료**(최소 버전).
  - [x] **U2 홈 지도** — ChungbukMap(11지역 SVG path 파싱·탭 히트테스트·진행도 0/1/2+ 색칠)·통계 요약. **1차 구현 완료**(진행중/최근완료 카드는 미구현, 아래 참고).
  - [x] **U3 퀘스트(목록·지역·상세·인증)** — 목록(유형 필터)·지역퀘스트·상세·인증(사진/GPS는 즉시 완료, OX퀴즈는 정오답 판정), 완료 시 지도·타임라인·리워드 반영. **1차 구현 완료**(스캔 연출 없음, 아래 참고).
  - [x] **U4 타임라인+프로필** — 타임라인(완료 기록)·마이·내정보수정(탈퇴 모달). **1차 구현 완료**(공유카드는 미구현).

## 구현된 항목

- [x] U0~U4 전체 화면 1차 구현(최소 버전) + 앱 셸·라우팅(GoRouter `StatefulShellRoute`)·전역 상태(Riverpod)·정적 데이터·ChungbukMap 위젯
- [x] `flutter analyze` 이슈 0, `flutter build web` 성공
- [x] Playwright로 전체 10개 라우트(스플래시~마이) 웹 렌더 스크린샷 검증, 콘솔/페이지 에러 0건
- [x] `frontend/Dockerfile`·`frontend/docker-compose.yml` 추가(Flutter SDK 미설치 환경 보조용), README 실행 방법에 반영

## 미구현 / 남은 항목

- [ ] 나머지 화면(마이 등)의 Figma 대조 — 스플래시·여행 목록·회원가입·퀘스트 상세·여행 타임라인·사진/GPS 인증·내 정보 수정·홈(지도)·공유 카드·지역 개요/퀘스트 선택은 Figma 스펙으로 교정 완료, 나머지는 여전히 프로토타입(`prototype.dc.html`) 기준 1차 버전
- [ ] 퀘스트 상세의 보상(P) 표시 — 2026-07-08 Figma 스펙엔 보상 노출이 없어 화면에서 제거함. 노출 위치를 다시 정하면 복원 필요
- [ ] 퀘스트 인증 실제 카메라/GPS 연동 — 사진 선택·GPS 위치 확인은 UI만 흉내(사진은 탭하면 바로 "선택됨" 처리, GPS는 정적 진행바), 실제 센서/권한 연동은 후속 작업
- [ ] 공유 카드의 실제 이미지 내보내기·링크 생성·OS 공유 시트 연동 — 지금은 버튼 탭 시 토스트만 표시
- [ ] 퀘스트 선택 화면의 검색은 제목 부분 일치 필터만 구현(간단한 클라이언트 필터, 실 검색엔진 아님)
- [ ] dev PR
- [ ] iOS/Android 실기기·시뮬레이터 빌드 검증(Xcode/Android 툴체인 미설치, 웹으로만 검증함)
- [ ] `pre-commit` 프레임워크·git 훅 설치(`uv tool install pre-commit && pre-commit install --install-hooks`) — Claude Code 자체 훅은 별개로 이미 동작 중

## 알려진 한계 / TODO

- **임시 구현 예정(후속 교체 대상)**: 카카오 로그인은 UI 스텁, 데이터는 정적 mock, 진행 상태는 메모리(앱 재실행 시 초기화), 사진/GPS 인증은 모의 동작. 실제 [Kakao+JWT](../../conventions/auth-security.md)·백엔드 API·[TourAPI](../../conventions/external-apis.md) 연동은 후속 스펙으로 분리.
- **커밋 전 검사는 네이티브 유지**: 프론트엔드 검사는 기존처럼 **pre-commit 프레임워크의 `dart format`·`flutter analyze` 훅**을 그대로 쓴다(백엔드가 `uv run`으로 네이티브 실행하는 것과 동일한 패턴). 이 훅은 flutter/dart가 시스템 PATH(brew `/opt/homebrew/bin` 등)에 있어야 동작하므로, 작업 전 Flutter SDK를 네이티브로 설치한다(Docker는 웹 빌드/테스트 보조용으로만 `frontend/Dockerfile`·`frontend/docker-compose.yml`에 남겨둠).
- **반응형 범위**: KAN-021 티켓에 "600px 브레이크포인트 웹 반응형"이 언급됐으나, 이 앱은 Flutter 모바일 앱이 SOT이므로 기존 `plan.md`의 **모바일 단일 폼팩터(392×812 기준, 상대 단위로 확장 가능하게 구현)** 요구사항을 그대로 따른다. 별도의 웹 브레이크포인트 전략은 도입하지 않는다.
- ~~**프로토타입 버전 차이**: 원본 zip의 `다채로울지도.dc.html`은 travel 화면을 "진행 중/지난 여행"으로 재설계하다 만 미완성본이라, 일관된 `다채로울지도-standalone-src.dc.html`(travel = 유형 필터 퀘스트 목록)을 채택했다.~~ → **2026-07-08 해결**: 사용자가 공유한 Figma 와이어프레임이 정확히 이 "진행 중/지난 여행" 구조였음이 확인되어, `features/travel/travel_list_screen.dart`로 구현하고 하단 탭 순서도 여행/홈/마이로 변경함(아래 변경 이력 참고). 유형 필터 평면 목록(`quest_list_screen.dart`)은 폐기하지 않고 여행 목록 화면의 "전체 퀘스트" 버튼으로 보조 진입점을 남겼다.
- 테스트는 [코드 품질 컨벤션](../../conventions/code-quality.md)에 따라 초기 생략.

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-06-25 | 최초 작성 (계획) |
| 2026-06-25 | U0~U4 구현 완료 기록 (이후 gitignore 버그로 소스 미커밋·유실 확인, 아래 참고) |
| 2026-06-26 | 커밋 전 검사를 pre-commit(dart format/analyze)로 통합, code-quality 컨벤션 갱신 |
| 2026-07-05 | `frontend/lib` 코드 유실 확인(gitignore 버그, `c9c00b6`으로 원인만 수정됨). KAN-021을 추적 티켓으로 U0부터 재구현 결정. 반응형은 기존 모바일 단일 폼팩터 요구사항 유지, 커밋 전 검사는 네이티브 Flutter 설치로 통일 |
| 2026-07-05 | U0~U4 1차 구현(최소 버전) 완료. `flutter analyze` 0 issues, `flutter build web` 성공, Playwright로 10개 라우트 스크린샷 검증(에러 0). 미구현 항목은 위 "미구현/남은 항목" 참고 |
| 2026-07-06 | 스플래시 화면을 Figma 스펙으로 교정(민트그린 히어로 카드, 카카오 브랜드 옐로우 버튼 `#FEE500`+진회색 텍스트 `#191919`, 새 카피). `AppColors`에 스플래시 전용 토큰 추가 |
| 2026-07-08 | Figma "여행 목록" 화면 반영: 하단 탭바를 **여행/홈/마이** 순서로 변경(원형 배지 스타일 nav로 교체), `features/travel/travel_list_screen.dart` 신규(지역=여행 매핑, 진행중/지난 분류, 유형 배지·진행도 표시). 유형 필터 평면 목록은 `/quests`로 보조 진입점 유지. 공유카드 진입 위치를 홈으로 확정(화면은 미구현) |
| 2026-07-08 | Figma "회원가입"·"퀘스트 상세"·"여행 타임라인" 반영. 회원가입: 이름·생년월일·이메일 필드 + 약관 동의 체크박스(필수 미체크 시 다음 버튼 비활성) + 3단계 진행바 추가. 퀘스트 상세: 관광지 이미지 placeholder·지역/유형/인증방식 배지·"퀘스트 조건" 카드·공통 CTA 문구로 재구성(보상 표시는 스펙에 없어 제거). 여행 타임라인: 월별 필터 칩 + 월별 그룹 헤더 + 점/선 연결 카드로 재구성. 부수적으로 지역/여행/퀘스트 상세로의 내비게이션을 `context.go()`→`context.push()`로 바꿔 뒤로가기 버튼이 정상 동작하도록 수정(`AppBackButton` 공용 위젯 추가) |
| 2026-07-08 | Figma "GPS 인증"·"사진 인증" 화면 반영: `features/quests/quest_verify_screen.dart` 신규(`/quest/:id/verify`). 퀘스트 상세의 "퀘스트 인증하기" 버튼이 사진/GPS 유형에 따라 각각의 인증 화면으로 이동하도록 변경(기존엔 버튼 탭 시 즉시 완료 처리). 사진 인증은 업로드 박스·카메라/갤러리 버튼 탭으로 "선택됨" 상태를 흉내 내 완료 버튼을 활성화하고, GPS 인증은 지도 미리보기·진행바가 있는 상태 카드를 보여준 뒤 탭하면 바로 완료 처리 |
| 2026-07-08 | Figma "내 정보 수정" 반영: 프로필 아이콘, 이메일 필드(변경불가·비활성 스타일), 여행 DNA 유형 행(탭 시 설문 재응시로 이동), 아웃라인 스타일 회원 탈퇴 버튼 추가. 회원가입 화면의 필드 위젯을 `core/widgets/app_form_field.dart`(`AppFormField`)로 공용화해 내 정보 수정과 재사용 |
| 2026-07-09 | Figma "지도"(홈)·"공유 카드 만들기" 반영. 지도 색칠 색상을 3단계로 변경(미방문 `#CCCCCC`/1개 완료 `#97C459`/2개 이상 `#378ADD`, 기존엔 2개 이상이 진초록이었음) + 범례(`MapLegend`) 추가. 홈 화면에 완료지역·진행률(진행바) 스탯, 진행중 여행+DNA 요약 카드, 최근 완료 섹션(최대 3건, 더보기→타임라인), 우측 상단 공유 아이콘을 추가. `features/home/share_card_screen.dart` 신규(`/share`) — 지도+DNA/지도만/DNA만 스타일 선택, 이미지저장/링크복사/공유는 토스트로만 흉내. **하단 탭바는 이번 Figma export가 4탭(홈/퀘스트/지도/마이)을 보여줬지만 같은 날 다른 화면들과 다르므로 기존 3탭(여행/홈/마이) 구조를 유지하고 사용자에게 확인 요청함** |
| 2026-07-09 | 사용자 확인 완료: 하단 탭바는 **3탭(여행/홈/마이) 유지**, 퀘스트는 별도 탭이 아니라 지도/여행 목록을 통해서만 진입. Figma "여행하기"·"퀘스트"(선택)·"퀘스트 상세"(GPS 인증 예시) 반영해 지역 진입 흐름을 재구성: `/region/:id` → **`RegionOverviewScreen`**(지역 이미지, DNA 요약 카드, 추천 퀘스트 2건 — 사용자 DNA 유형 우선, "퀘스트 선택하기" CTA), `/region/:id/quests` → **`RegionQuestSelectScreen`** 신규(검색·유형 필터, 카드 탭으로 단일 선택, 카드의 "퀘스트 설명"으로 퀘스트 상세 진입, "여행 시작하기"로 선택된 퀘스트의 인증 화면 직행). 기존 `region_quests_screen.dart`(퀘스트 평면 목록)는 삭제하고 위 두 화면으로 대체 |
| 2026-07-09 | Figma "사진 검증 결과" 반영: `features/quests/photo_verify_result_screen.dart` 신규(`/quest/:id/verify/result`). 사진 인증만 완료 즉시 지도로 돌아가던 것을 이 결과 화면(AI 검증 결과 — 장소 일치도 94%/사진 진위 통과/촬영 시간 24시간 이내는 고정값, 실제 분석 없음 — + "퀘스트 완료!" 안내)을 거치도록 변경, "지도에서 확인하기"로 홈까지 이동. GPS/OX퀴즈 인증은 기존과 동일(완료 즉시 이전 화면들로 pop). 홈 화면에서 완료 후 상태(진행중 여행 카드·지도 색칠·최근 완료)가 실제로 갱신되는 것을 스크린샷으로 종단 검증 |
| 2026-07-09 | **"여행 시작하기" 흐름 재설계**(사용자 수정 요청). 퀘스트 선택은 단일→**다중 선택**으로 변경. "여행 시작하기"는 더 이상 퀘스트를 그 자리에서 수행시키지 않고, 선택한 퀘스트를 지역의 "여행"으로 등록(`ProgressState.tripQuests`, `ProgressNotifier.startTrip`)한 뒤 **여행 탭으로 이동**한다 — 그 즉시 해당 지역이 "진행중인 여행"에 들어간다(완료 퀘스트 0개여도). `RegionOverviewScreen`은 여행 시작 여부에 따라 분기: 시작 전엔 기존 추천 퀘스트+선택하기, 시작 후엔 **"내 여행 퀘스트"** 목록(미완료 탭 시 인증 화면 직행, "퀘스트 더 선택하기"로 추가 가능)을 보여준다. **퀘스트 상세 화면에서 인증/수행 버튼을 완전히 제거**(정보 전용으로 전환) — OX퀴즈 인터랙션은 `QuestVerifyScreen`으로 이동(`_QuizVerifyBody` 신규). 여행 목록의 진행중/지난 분류도 지역 퀘스트 완료 개수 기준에서 `tripQuests` 기준(`ProgressState.tripStatusOf`, `RegionTripStatus` enum)으로 변경. 홈의 완료지역/진행률 스탯과 지도 색칠은 기존 지역별 완료 개수 기준을 그대로 유지(별개 지표로 명확히 구분). 전체 흐름(다중 선택→여행 시작→여행 탭 반영→여행하기의 "내 여행 퀘스트"→탭하면 인증 화면 직행)을 스크린샷으로 종단 검증 |
| 2026-07-16 | KAN-28 프론트 디테일 3건. ① **홈 추천 여행지 배너**: 사용자 DNA 유형과 같은 유형 퀘스트가 가장 많은 **미시작 지역**을 골라(동률이면 전체 퀘스트 수) 홈 스탯 아래에 DNA 그라데이션 배너로 노출, 탭하면 해당 지역 개요(`/region/:id`)로 이동. 시작 안 한 지역이 없으면 배너 숨김. ② **퀘스트 정적 데이터 확장**: 18개 → **55개**(11개 시·군 각 5개, 유형·인증방식 고르게 배분, OX퀴즈 7개). ③ **여행 시작 시 이름·날짜 입력**: "여행 시작하기" 버튼이 바텀시트(여행 이름 기본값 `tripTitleFor`, `showDateRangePicker`로 시작·종료일)를 띄우고, 입력 값은 `ProgressState.tripInfo`(`TripInfo` — name/startDate/endDate)로 보관. 여행 목록 카드가 입력한 이름·기간을 표시. 이미 시작한 여행에 퀘스트를 더 담을 때는 시트 없이 기존 이름·날짜 유지. 날짜 피커 한국어화를 위해 `flutter_localizations` 의존성 추가(백엔드 `journeys.start_date`·`end_date`와 대응 — [010-journey](../010-journey/description.md)). 세 기능을 검증하는 위젯 테스트 `test/kan28_features_test.dart` 추가(첫 테스트 도입). pubspec Dart SDK 하한을 `^3.12.2`→`^3.12.0`으로 완화 — README 요구사항(3.12+)과 일치시키고 Docker 컨테이너(flutter:stable=3.44.0, Dart 3.12.0)에서도 pub 해석이 가능하게 함 |
| 2026-07-17 | **퀘스트 정적 데이터 55→220개**(시·군별 20개). 한국관광공사 TourAPI(KorService2 `areaBasedList2`·`detailCommon2`)에서 시·군별 관광지·문화시설·레포츠·음식점을 수집해 지역당 15개씩 생성(수제 1~5번 유지, 6~20번 생성분). 분류코드(cat1/cat2)→유형 매핑·중복 명소 제외·유형 쿼터·공식 소개문 요약은 생성 스크립트 `backend/scripts/generate_frontend_quests.py`에 구현(재실행 가이드 포함). 홈 배너 위젯 테스트는 데이터 개수에 의존하지 않게 기대값을 kQuests에서 계산하도록 변경. TourAPI 활용신청 현황·사용법은 [external-apis.md](../../conventions/external-apis.md)에 문서화 |
| 2026-07-21 | 지도 색칠을 3단계(0/1/2+, 2+는 파랑 `#378ADD`)에서 **연속 채도**로 변경(KAN-44, dev 브랜치 반영분 — 이 문서에는 그동안 미기록). `ProgressState.regionSaturation(regionId)`가 그 지역 완료 퀘스트 개수를 전체 퀘스트 개수로 나눈 비율(0.0~1.0)을 반환하고, `mapFillColors`가 회색(`mapEmpty`)→진초록(`primaryDark`) 사이를 `Color.lerp`로 보간한다. 완료 개수(`ProgressState.regionProgress`)는 `ProgressNotifier.completeQuest`에서 즉시 +1(낙관적 갱신)되고, 앱 진입 시 백엔드 `GET /users/me/map`의 `completed_count`로 동기화된다(`syncRegionProgressFromServer`, [020-frontend-map-sync]) — KAN-31의 지도 API 연동과 KAN-44의 연속 채도를 이번에 재정합함. `MapLegend`도 3단계 스와치 대신 0%~100% 그라데이션 바로 변경 |
| 2026-07-21 | `regionSaturation` 계산 기준 변경(사용자 요청) — 지역 전체 퀘스트 개수 대비 비율 대신, **완료 개수 ÷ 6(고정값)**, 6개 이상이면 100%로 클램프. 지역마다 정적 퀘스트 개수가 1~3개로 제각각이라 비율 기준으로는 퀘스트가 적은 지역만 완료 1~2개로 바로 100%가 돼버리는 문제가 있었음(테스트로 확인). 고정 기준선 6은 `ProgressState._saturationCap`. `completed_count`가 재방문(KAN-46)으로 정적 퀘스트 개수를 넘어 계속 늘 수 있다는 점과도 더 맞음 |
| 2026-07-23 | KAN-28 브랜치와 dev 병합 정합: 홈 추천 여행지 배너를 코치마크(KAN-040)·연속 채도 지도(KAN-44) 구조의 새 홈 레이아웃(스탯 아래) 위치에 유지, 퀘스트 선택의 "여행 시작하기/퀘스트 추가하기" 버튼(KAN-42·46)이 새 여행일 때만 이름·기간 입력 시트(KAN-28)를 거치도록 `_startTrip`으로 연결(이미 시작·완료된 지역은 시트 없이 추가). 참고: KAN-44의 채도 기준선(`_saturationCap`=6) 논의 당시 지역별 퀘스트가 1~3개였으나 현재는 TourAPI 확장으로 지역당 20개 |
