# [구현 수준] 공유 카드 실사용화 (지도 미리보기·네이티브 공유 시트·공유 랜딩 페이지)

| 항목 | 내용 |
|------|------|
| 상태 | 계획 |
| 최종 업데이트 | 2026-08-02 |

## 구현 규모 / 단위 분할

- **규모 판단**: 단위로 나눠 구현 — 근거: 프론트(위젯 교체·공유 시트)와 백엔드(HTML 라우트)가 서로 독립적으로 검증 가능하고, 각 단위가 별도 커밋/검증 지점을 갖는 게 안전하다.
- **구현 단위** (순서대로):
  - [ ] 1) 프론트 지도 미리보기 실제 위젯 교체 — 완료 기준: `share_card_screen.dart`에서 실제 `ChungbukMap`이 현재 채도 상태로 렌더링됨(placeholder 텍스트 제거).
  - [ ] 2) 프론트 네이티브 공유 시트 + 링크 복사 — 완료 기준: `share_plus`로 실제 OS 공유 시트가 뜨고, 링크 복사가 실제 클립보드에 반영됨(토스트 스텁 제거).
  - [ ] 3) 안드로이드 커스텀 스킴 등록 — 완료 기준: `colortrip://` intent-filter 추가로 앱이 그 스킴 링크로 실행됨.
  - [ ] 4) 백엔드 공유 랜딩 페이지 — 완료 기준: `GET /share/{share_code}`가 실제 데이터로 HTML을 렌더링하고(앱에서 열기/다운받기 버튼 포함), 존재하지 않는 코드는 404 페이지를 반환함.

## 구현된 항목
- (아직 없음)

## 미구현 / 남은 항목
- [ ] 지도 미리보기 위젯 교체
- [ ] `share_plus` 연동 및 링크 복사 실제 구현
- [ ] 안드로이드 커스텀 스킴(`colortrip://`) intent-filter
- [ ] `GET /share/{share_code}` HTML 라우트(앱에서 열기/다운받기 버튼) 및 404 처리
- [ ] `030-share-card` 비목표 문구 정리 및 상호 링크

## 알려진 한계 / TODO
* 앱스토어 CTA 링크는 placeholder — 실제 앱 배포 후 교체 필요.
* 공유 랜딩에는 실제 지도 이미지가 없음(텍스트/뱃지 카드만) — Non-Goal로 결정됨, 필요해지면 별도 스펙으로 재검토.

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-08-02 | 최초 작성 |
